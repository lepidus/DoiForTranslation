variables:
  PLUGIN_NAME: 'doiForTranslation'

include:
  - project: 'documentacao-e-tarefas/modelosparaintegracaocontinua'
    ref: stable-3_5_0
    file:
      - 'templates/groups/pkp_plugin.yml'
      - 'templates/groups/ojs/unit_tests.yml'

# The browser suite still targets the OJS 3.3 editorial interface.
# Re-enable the Cypress template when those workflows are ported to OJS 3.5.
